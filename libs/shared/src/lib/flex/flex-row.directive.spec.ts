import { FlexRowDirective } from './flex-row.directive';

describe('FlexRowDirective', () => {
  it('should create an instance', () => {
    const directive = new FlexRowDirective();
    expect(directive).toBeTruthy();
  });
});
