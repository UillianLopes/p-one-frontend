import { FlexColumnDirective } from './flex-column.directive';

describe('FlexColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new FlexColumnDirective();
    expect(directive).toBeTruthy();
  });
});
