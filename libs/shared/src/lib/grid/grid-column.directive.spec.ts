import { GridColumnDirective } from './grid-column.directive';

describe('GridColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new GridColumnDirective();
    expect(directive).toBeTruthy();
  });
});
