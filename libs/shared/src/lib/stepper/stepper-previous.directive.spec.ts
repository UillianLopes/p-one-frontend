import { StepperPreviousDirective } from './stepper-previous.directive';

describe('StepperPreviousDirective', () => {
  it('should create an instance', () => {
    const directive = new StepperPreviousDirective();
    expect(directive).toBeTruthy();
  });
});
