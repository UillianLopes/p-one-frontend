import { InvalidFeedbackDirective } from './invalid-feedback.directive';

describe('InvalidFeedbackDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidFeedbackDirective();
    expect(directive).toBeTruthy();
  });
});
