import { SidenavTriggerDirective } from './sidenav-trigger.directive';

describe('SidenavTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new SidenavTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
